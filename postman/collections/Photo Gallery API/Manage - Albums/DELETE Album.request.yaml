$kind: http-request
name: DELETE Album
method: DELETE
url: '{{baseUrl}}/api/manage/albums/{{albumId}}'
order: 5000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status is 204', function () {
        pm.response.to.have.status(204);
      });
